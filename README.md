# kaggle-image_classifiers

This repository contains some image datasets from kaggle (including competitions). CNNs were trained using these datasets using PyTorch for the purpose of image classification.
